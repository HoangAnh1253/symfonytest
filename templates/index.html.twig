{% extends "base.html.twig" %}

{% block body %}
	<h1>
		Welcome to symfony
		{{ title }}
	</h1>

	<div>
		Categories:
	</div>
	<div>
		{% for category in categories %}
			<li>
				{{ category.title }}
				-
				{{ category.desc }}
			</li>
		{% endfor %}
	</div>
    <img src="{{ asset('build/images/x.5ad31f72.jpg') }}" alt="">
{% endblock %}
